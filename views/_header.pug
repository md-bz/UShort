header.header 
    nav.nav
        a.nav__el(href="/") UShort
    nav.nav.nav--user 
        if user
            a#logout.nav__el.nav__el--logout Log out
            a.account-link(href="/me") 
                img(src=`/img/users/${user.photo}`, alt=`${user.name} photo`)
           
        else                
            a.sign-in(href="/signup") Sign up
            a.login(href="/login") Log in